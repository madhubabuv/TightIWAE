#1). Install these libraries:
### pytorch gpu -> https://varhowto.com/install-pytorch-cuda-10-0/

# setup CUDA version 10.0
cat /usr/local/cuda/version.txt

pip install torch==1.4.0 torchvision==0.5.0
pip install h5py
pip install matplotlib

#2.) Try Pytorch with GPU:

import torch
import sys
print('A', sys.version)
print('B', torch.__version__)
print('C', torch.cuda.is_available())
print('D', torch.backends.cudnn.enabled)
device = torch.device('cuda')
print('E', torch.cuda.get_device_properties(device))
print('F', torch.cuda.get_device_capability())
print('G', torch.tensor([1.0, 2.0]).cuda())

# Note:
# Tesla K40m has compute capacity 3.5 < doesn't work
# Tesla P100 < works!